// create a note taking something

const notes = ['Note 1', 'Note 2', 'Note 3']

console.log(notes[0]) // first value is indexed at ZERO
console.log(notes.length) // return the amount of items in the notes array
console.log(notes[notes.length - 1])